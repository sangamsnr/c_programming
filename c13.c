#include<stdio.h>
void print array(int arr[],int size);
void sort array(int arr[],int size);
int main()
{
    int array[]={4,8,2,0,1};
    int size=size of (array)/size of (array[0]);
    sortarray(array,size);
    printarray(array,size);
    retrun 0;

}
void printarray(inr arr[],int size)
{
    for(int i=0;i<size;i++)
    {
        printf("%d",arr[i]);
    }
    printf("\n");
    
}
void sortarray(int arr[],int size)
{
    
} 
